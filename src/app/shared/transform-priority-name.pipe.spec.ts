import { TransformPriorityNamePipe } from './transform-priority-name.pipe';

describe('TransformPriorityNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformPriorityNamePipe();
    expect(pipe).toBeTruthy();
  });
});
