import { TransformGlobalRoleNamePipe } from './transform-global-role-name.pipe';

describe('TransformGlobalRoleNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformGlobalRoleNamePipe();
    expect(pipe).toBeTruthy();
  });
});
