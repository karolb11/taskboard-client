import { TransformTaskStateNamePipe } from './transform-task-state-name.pipe';

describe('TransformTaskStateNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformTaskStateNamePipe();
    expect(pipe).toBeTruthy();
  });
});
